public interface Forme {
    /*Interface modélisant une forme dans un espace 2D*/

    //FOnction permettant d'indiquer si la forme contient la position (x,y)
    public boolean estContennu(double x, double y);
}
